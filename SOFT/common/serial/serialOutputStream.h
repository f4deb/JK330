#ifndef SERIAL_STREAM_H
#define SERIAL_STREAM_H




/**
 * Init an outputStream arround serial with index "serialPortIndex".
 */
void initSerialOutputStream(const serialPortIndex, int baudeRate);

#endif